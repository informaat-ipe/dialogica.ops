{
	"name": "watchdog",
	"version": "0.0.0",
	"private": "true",
	"description": "Ghetto watchdog polling a list of servers every 5 minutes, sending an email if the return code is not 200 OK.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"repository": "",
	"author": "",
	"license": "BSD",
	"readmeFilename": "README.md",
	"dependencies": {
		"nodemailer": "~0.4.1",
		"irc": "~0.3.6"
	}
}
